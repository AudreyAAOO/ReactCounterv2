// const Button = (props) => {
// props.counters.map((element, index) => {
//             const functionReset = () => {
//                 let copy = [...props.counters];
//                 copy[props.index] = 0;
//                 console.log(props.element);
//                 props.setCounters(copy);
//             }})

//             return (<>

//                 <button name="props.name" className="props.className" id="test" onClick={functionReset}> recgfcgset </button>

 
//   </>)
  
// }
// export default Button;